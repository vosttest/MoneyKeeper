<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MoneyKeeper.Token.ViewModels"
             x:Class="MoneyKeeper.Token.MainPage">
    <ContentPage.BindingContext>
        <vm:UserVM/>
    </ContentPage.BindingContext>
    <StackLayout>
        <Label Text="Welcome to Money Keeper Token Application"
               VerticalOptions="CenterAndExpand"
               HorizontalOptions="CenterAndExpand"/>
        <Label x:Name="lblDate"/>
        <Label x:Name="lblSecond"/>
        <Label x:Name="lblToken"/>
        <Button Text="Get Token"
                TextColor="White"
                BackgroundColor="Blue"
                HeightRequest="50"
                VerticalOptions="Start"
                Command="{Binding GetTokenCmd}"/>
    </StackLayout>
</ContentPage>