<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MoneyKeeper.Main.ViewModels"
             x:Class="MoneyKeeper.Main.Views.Accounts"
             Title="{Binding Title}">
    <ContentPage.BindingContext>
        <vm:AccountVM/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Add New"
                    Grid.Row="1"
                    Grid.Column="0"
                    TextColor="White"
                    BackgroundColor="#1cb09a"
                    Margin="20,10,20,0"
                    Command="{Binding AccountAddCmd}"/>
            <SearchBar Placeholder="Search by account name"
                       SearchButtonPressed="OnSearchBarButtonPressed"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>